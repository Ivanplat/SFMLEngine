#include "pch.h"
#include "RawObject.h"

void SRawObject::RunBinedFunction(std::function<void()>& F)
{
	/*auto it = std::find(BindedEvents.begin(), BindedEvents.end(), F);
	if (it != BindedEvents.end())
	{
		(*(it))();
	}*/
}
